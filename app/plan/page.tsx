import ProviderBand from "../components/ProviderBand";
import RlpReveal from "../components/RlpReveal";

// The Retirement Life Plan — the document the member sees after completing the
// seven Stage 4 modules, and returns to thereafter.
export default function PlanPage() {
  return (
    <>
      <ProviderBand />
      <RlpReveal />
    </>
  );
}
